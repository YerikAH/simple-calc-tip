export type TipPor = {
  numberSelect: number;
  active: boolean;
};
export type StyleErr = {
  border: string;
};

export type PropsResult = {
  tipAmount: string;
  totalPerson: string;
  handleReset: () => void;
};
